import { createRouter, createWebHistory } from 'vue-router';

const router = createRouter({
  history: createWebHistory(),
  // routes: [{ path: '페이지명', name: '별칭', component: RouterView 에 보여질 컴포넌트 }]
  routes: []
});
export default router;